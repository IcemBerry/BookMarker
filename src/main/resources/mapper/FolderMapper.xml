<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dao.FolderMapper">
  <resultMap id="BaseResultMap" type="model.Folder">
    <id column="folder_id" jdbcType="INTEGER" property="folderId" />
    <result column="folder_name" jdbcType="VARCHAR" property="folderName" />
    <result column="book_id" jdbcType="INTEGER" property="bookId" />
  </resultMap>
  <sql id="Base_Column_List">
    folder_id, folder_name, book_id
  </sql>
  <select id="selectByPrimaryKey" parameterType="java.lang.Integer" resultMap="BaseResultMap">
    select 
    <include refid="Base_Column_List" />
    from folder
    where folder_id = #{folderId,jdbcType=INTEGER}
  </select>
  <delete id="deleteByPrimaryKey" parameterType="java.lang.Integer">
    delete from folder
    where folder_id = #{folderId,jdbcType=INTEGER}
  </delete>
  <insert id="insert" parameterType="model.Folder">
    insert into folder (folder_id, folder_name, book_id
      )
    values (#{folderId,jdbcType=INTEGER}, #{folderName,jdbcType=VARCHAR}, #{bookId,jdbcType=INTEGER}
      )
  </insert>
  <insert id="insertSelective" parameterType="model.Folder">
    insert into folder
    <trim prefix="(" suffix=")" suffixOverrides=",">
      <if test="folderId != null">
        folder_id,
      </if>
      <if test="folderName != null">
        folder_name,
      </if>
      <if test="bookId != null">
        book_id,
      </if>
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides=",">
      <if test="folderId != null">
        #{folderId,jdbcType=INTEGER},
      </if>
      <if test="folderName != null">
        #{folderName,jdbcType=VARCHAR},
      </if>
      <if test="bookId != null">
        #{bookId,jdbcType=INTEGER},
      </if>
    </trim>
  </insert>
  <update id="updateByPrimaryKeySelective" parameterType="model.Folder">
    update folder
    <set>
      <if test="folderName != null">
        folder_name = #{folderName,jdbcType=VARCHAR},
      </if>
      <if test="bookId != null">
        book_id = #{bookId,jdbcType=INTEGER},
      </if>
    </set>
    where folder_id = #{folderId,jdbcType=INTEGER}
  </update>
  <update id="updateByPrimaryKey" parameterType="model.Folder">
    update folder
    set folder_name = #{folderName,jdbcType=VARCHAR},
      book_id = #{bookId,jdbcType=INTEGER}
    where folder_id = #{folderId,jdbcType=INTEGER}
  </update>
</mapper>